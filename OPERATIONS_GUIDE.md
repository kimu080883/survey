# 運転日報アプリ v4 運用手順

## 社員

1. 日報入力を開く
2. 自分の氏名を選ぶ
3. 車両番号を選ぶ
4. 自動表示された開始距離と実メーターを確認する
5. 違う場合のみ「実際のメーターと違う場合」を押し、距離と理由を入力する
6. 運転終了後に終了距離、アルコール確認、利用内容を入力する
7. 「日報を確定保存」を押す
8. 同時入力警告が出た場合は、再取得された開始距離を確認して入力し直す

## 管理者

1. 月次管理を開く
2. 対象月を選択して「集計」を押す
3. 車両別の月初・月末・月間距離・日報件数・運転者数を確認する
4. 距離修正件数がある車両は明細を確認する
5. 「月次明細CSVを出力」で保存する
6. 必要に応じて印刷・PDF化し、Boxの月次フォルダへ保管する

## 試験から本番への切替

1. API_CONTRACT.mdどおりに本番データベースAPIを用意する
2. ダミー社員・車両で結合試験する
3. app-config.jsを `mode: "api"` に変更する
4. `apiBaseUrl` を本番APIへ変更する
5. 社員・車両の実マスタを登録する
6. 車両ごとの開始時点メーターを登録する
7. 管理者・社員それぞれで保存試験する
8. 月次CSVの値を実メーターと照合して運用開始する

## 注意

- 本番では社員認証と管理者権限をAPI側で必ず確認する
- 画面の表示制御だけを権限制御として使用しない
- 日報と距離更新は同一データベーストランザクションで処理する
- 過去データは削除せず、修正履歴を残す
